% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_bulk_operations.R
\name{list_imports}
\alias{list_imports}
\title{List Import Operations}
\usage{
list_imports(index, limit = 100, pagination_token = NULL)
}
\arguments{
\item{index}{Name of the index}

\item{limit}{Maximum number of operations to return (default: 100)}

\item{pagination_token}{Token for fetching the next page of results}
}
\value{
List with http response, content (import operations), and status_code.
  Content includes:
  - data: list of import operations with id, status, uri, etc.
  - pagination: list with next token if more results exist
}
\description{
Lists all recent and ongoing import operations for an index.
}
\examples{
\dontrun{
# List recent imports
result <- list_imports("my-index")

# Paginate through results
result <- list_imports("my-index", limit = 10)
if (!is.null(result$content$pagination$next)) {
  next_page <- list_imports("my-index",
    pagination_token = result$content$pagination$next)
}
}
}
